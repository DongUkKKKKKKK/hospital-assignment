# 병원 검색 서비스 (Hospital Map Search)

사용자의 현재 위치를 기준으로 주변 병원을 지도에 표시하고 거리순으로 정렬하여 보여주는 프론트엔드 웹 애플리케이션입니다.
최종적으로 Google Maps API를 기반으로 3,000건 이상의 모의 데이터를 고성능으로 시각화합니다.

## 🚀 실행 방법

본 프로젝트는 최신 React(18)와 Vite 환경에서 구성되었습니다. 프로젝트 실행 전, 반드시 컴포넌트 의존성을 설치하고 루트 경로에 `.env.local` 파일을 확인해주세요.

```bash
# 1. 패키지 및 의존성 환경 설치 (Google Maps 필수 모듈 포함)
npm install
npm install @react-google-maps/api @googlemaps/markerclusterer

# 2. 구글 지도 인증 키 환경 변수 세팅 (.env.local)
# 사용자 본인의 Google Maps API Key가 기입되어 있어야 합니다.
# VITE_GOOGLE_MAPS_API_KEY="YOUR_KEY_HERE"

# 3. 로컬 개발 서버 환경 실행
npm run dev
```

## 🛠 기술적 의사결정 (과제 요구사항 준수 및 최적화 전략)

### 1. 비동기 통신 모사 환경 (MSW)
외부 백엔드 API가 연결되어 있지 않은 조건에서 `hospital.json` 데이터를 현실적인 비동기 송수신 형태로 불러오기 위해 **MSW (Mock Service Worker)**를 채택했습니다. 단순 파일 로드가 아닌, **10% 에러 발생 조건과 500ms 딜레이**를 주입함으로써, 실제 네트워크 지연과 예외가 발생하는 실무 환경을 재현했습니다. 이를 통해 로딩 Skeleton UI 및 에러 재시도 버튼 같은 방어 로직을 효과적으로 테스트할 수 있었습니다.

### 2. 전역 상태 기반 양방향 상호작용
우측 구글 지도 내의 마커 선택 상태와 좌측 리스트 노출 상태를 즉각 동기화시키기 위해 **Redux Toolkit**을 사용해 관심사를 묶었습니다.
사용자가 마커를 클릭해 `selectedHospitalId` 상태가 전역으로 갱신되면, 좌측 패널에서도 동일 아이템이 감지되어 `scrollIntoView`가 부드럽게 호출되고, 아이템이 강조(Highlight)됩니다. 반대로 리스트에서 한 항목을 클릭하면 맵 인스턴스는 부드럽게(`panTo`) 이동합니다.

### 3. 하버사인(Haversine) 거리 정렬 최적화
모든 병원과 사용자 간 '직선거리'를 실시간 산출하여 정렬 기준(순위)으로 반영하기 위해 하버사인 공식을 적용했습니다. 배열을 순회하며 매 렌더링마다 3,000번씩 계산하는 리소스 낭비를 막고, 오직 사용자 위치값(`userLocation`)이나 필터값(`filter`)의 변동 시에만 계산이 트리거 되도록 **useMemo/useCallback** 처리하여 프레임 안정성을 확보했습니다.

---

## 🔥 트러블 슈팅 (Troubleshooting)

### 1. 3,000개 마커 렌더링 성능 과부하 최적화
- **문제**: 구글 지도 로드 직후 3,000개의 마커 노드가 DOM에 동시다발적으로 생성되어 UI 프리징 현상이 빚어졌으며, 같은 상가 단위로 중복된 좌표가 다수 병합되어 세부분포를 클릭조차 할 수 없었습니다.
- **해결 방안**: `@googlemaps/markerclusterer` 라이브러리를 바인딩하였습니다. 줌 레벨과 거리 범주(그리드 사이즈)에 맞춰 조밀한 마커 다발을 한 개의 클러스터로 추상화하여, 랜더링 속도를 선형 구조로 격감했습니다. 사용자는 줌인 과정 중 해소되는 클러스터를 쫓아 동일 공간 내 여러 병원을 시각적으로 쪼개볼 수 있습니다.

### 2. Geolocation API 거부 / 실패 시 Fallback UX
- **문제**: 브라우저 권한 설정으로 인해 사용자의 위치값(`getCurrentPosition`)을 제공받지 못해 무한 로딩 창에서 맵이 중단되는 엣지 케이스가 있었습니다.
- **해결 방안**: 거부 콜백 시 `alert` 안내를 우선적으로 띄운 직후, Redux Action을 통해 **서울시청(37.5665, 126.978)**을 예외 Fallback 위치 값으로 즉시 강제 수동 전달합니다. 이를 통해 애플리케이션의 거리 계산 및 지도 맵뷰 초기화 기능이 막히는 것을 방어했습니다.
